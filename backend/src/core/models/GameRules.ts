import {Board} from "./Board.js";
import {BornStates} from "../types.js";

export function generateNextBoard(currentState: Board) : Board {
    //APPLY RULES to CURRENTSTATE HERE
}



export function getBirthAndDeaths(currentState: Board, newState: Board) : BornStates {
    //Figure out a design for this
};